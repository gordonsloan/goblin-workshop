# Placeholder Audio

These generated WAV files are disposable starter-template audio.

- `menu_loop.wav` - soft synth menu loop.
- `menu_loop_extended.wav` - longer soft synth menu loop with a gradual build, gentle crescendo, and calm return.
- `ui_hover.wav` - subtle hover/focus sound.
- `ui_confirm.wav` - positive button press sound.
- `ui_back.wav` - softer return/back sound.

Replace or remove these files when a project has real audio assets.
